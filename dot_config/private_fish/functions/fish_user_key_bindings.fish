
  # ctrl+p make next on word completion
  bind --preset -M insert \cP nextd-or-forward-word
  # ctrl+o make prev on word completion
  bind --preset -M insert \cO prevd-or-backward-word

  bind --preset -M insert \c] accept-autosuggestion # execute

  bind --preset -M insert \ck kill-line

